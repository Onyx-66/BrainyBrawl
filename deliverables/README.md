# Brainy Brawl 1.0.2 — mobile test build

`BrainyBrawl-1.0.2.apk` is the current signed, installable testing APK for Android 8.0/API 26 or newer. Transfer it to your phone and open it to install. Package: `com.brainybrawl.app`; version code: 3. The online backend is configured and the selected background now appears on every application page.

This uses the existing development signing key. It is not a Play Store release. Production signing and remaining gates are recorded in `docs/RELEASE_CHECKLIST.md`. The APK is excluded from Git and the source-change ZIP.

Size: 30170598 bytes.

SHA-256: `594f88414231623d91cdff6560ee588b3d22a406c9e54369966341d45050bec1`
